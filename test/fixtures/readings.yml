# == Schema Information
#
# Table name: readings
#
#  id              :integer          not null, primary key
#  key             :string
#  value           :string
#  created_at      :datetime         not null
#  updated_at      :datetime         not null
#  device_id       :integer          not null
#  mqtt_message_id :integer          not null
#
# Indexes
#
#  index_readings_on_created_at                (created_at)
#  index_readings_on_device_id                 (device_id)
#  index_readings_on_device_id_and_created_at  (device_id,created_at DESC)
#  index_readings_on_device_key_created        (device_id,key,created_at DESC)
#  index_readings_on_key                       (key)
#  index_readings_on_mqtt_message_id           (mqtt_message_id)
#  index_readings_on_value                     (value)
#
# Foreign Keys
#
#  device_id        (device_id => devices.id)
#  mqtt_message_id  (mqtt_message_id => mqtt_messages.id)
#
motion_occupancy:
  key: "occupancy"
  value: "true"
  mqtt_message: motion_event
  device: motion_sensor

motion_battery:
  key: "battery"
  value: "95"
  mqtt_message: motion_event
  device: motion_sensor

motion_linkquality:
  key: "linkquality"
  value: "120"
  mqtt_message: motion_event
  device: motion_sensor

temp_temperature:
  key: "temperature"
  value: "22.5"
  mqtt_message: temp_reading
  device: temp_sensor

temp_humidity:
  key: "humidity"
  value: "45"
  mqtt_message: temp_reading
  device: temp_sensor

temp_pressure:
  key: "pressure"
  value: "1013"
  mqtt_message: temp_reading
  device: temp_sensor

door_contact:
  key: "contact"
  value: "false"
  mqtt_message: door_open
  device: door_sensor

door_battery:
  key: "battery"
  value: "70"
  mqtt_message: door_open
  device: door_sensor
